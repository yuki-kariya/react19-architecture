# ADR 1: FEアーキテクチャ

## Status



## Context

関連するデータや振る舞いを、論理的な単位で分割するための基準を設ける。

関連する要素だけでモジュールが構成したい（凝集度を高めたい）。

関連性が特に高いデータ・処理を分割しようとした場合に、各モジュールのソースコードを注意深く確認し、操作の実現に高度な結合が発生することは避けたい。

他モジュールに頻繁に依存されるモジュールの抽象度は高めたい。

逆に他モジュールに依存はするが、依存はされないモジュールの抽象度は高い必要はない。

## 